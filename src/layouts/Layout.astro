---
// src/layouts/Layout.astro
import "../styles/global.css";
export interface Props {
  title: string;
}
const { title } = Astro.props;
---

<!doctype html>
<html lang="es" class="m-0 p-0">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="color-scheme" content="only light" />
    <link rel="icon" type="image/png" href="/favicon-caballo.png" />
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>

    <!-- Vista previa al compartir el link (WhatsApp, Facebook, Telegram).
         Sin título ni descripción: se muestra solo la imagen. -->
    <meta property="og:type" content="website" />
    <meta property="og:url" content={Astro.site?.href} />
    <meta property="og:title" content="" />
    <meta property="og:description" content="" />
    <meta property="og:image" content={new URL("/og.jpg", Astro.site).href} />
    <meta property="og:image:secure_url" content={new URL("/og.jpg", Astro.site).href} />
    <meta property="og:image:type" content="image/jpeg" />
    <meta property="og:image:width" content="1200" />
    <meta property="og:image:height" content="630" />
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:image" content={new URL("/og.jpg", Astro.site).href} />
    <!-- Google Fonts: solo las tres familias que usa la invitación -->
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Great+Vibes&family=Crimson+Text:wght@400;600;700&family=Cinzel:wght@400;500;600;700&display=swap"
      rel="stylesheet"
    />

    <style>
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      /* Forzar modo claro y evitar dark mode del navegador */
      html {
        width: 100%;
        height: 100%;
        overflow-x: hidden;
        color-scheme: only light;
      }

      body {
        margin: 0;
        padding: 0;
        width: 100%;
        min-height: 100vh;
        overflow-x: hidden;
        color-scheme: only light;
        background-color: #f6ecd9;
      }

      /* Fondo global estático para todos los componentes.
         Va como capa fija detrás del contenido (más confiable que
         background-attachment: fixed, que falla en iOS). */
      body::before {
        content: "";
        position: fixed;
        inset: 0;
        z-index: -1;
        background-image: url("/imagenes/fondo3.png");
        background-size: cover;
        background-position: center center;
        background-repeat: no-repeat;
        pointer-events: none;
      }

      main {
        background: transparent !important;
      }
    </style>
  </head>
  <body class="min-h-screen m-0 p-0">
    <main class="m-0 p-0 overflow-x-hidden">
      <slot />
    </main>
  </body>
</html>
